apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx1.fullname" . }}-config
data:
  index.html: |
    <html>
    <head><title>Welcome</title></head>
    <body>
      <h1>{{ .Values.config.welcomeMessage }}</h1>
    </body>
    </html>
