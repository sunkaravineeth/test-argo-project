replicaCount: 1

image:
  repository: nginx
  tag: "1.25"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

config:
  welcomeMessage: "Hello from nginx1 Helm Chart!"

secret:
  username: admin
  password: admin123
